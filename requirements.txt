# Core
numpy>=1.26,<3
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
pyyaml>=5.4
pyarrow>=14          # HotpotQA parquet from the HF hub
httpx>=0.25          # dataset download + LLM providers
python-dotenv>=1.0
tqdm>=4.66

# Retrieval
sentence-transformers>=2.7   # genuine dense retrieval + cross-encoder reranker
torch>=2.1                   # CPU build is sufficient: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Evaluation / figures
matplotlib>=3.8
seaborn>=0.13

# API
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5

# Dev
pytest>=8
ruff>=0.5
nbformat>=5.9

# Optional live LLM providers (the code talks to their HTTP APIs via httpx; SDKs are NOT required)
# anthropic>=0.30
# openai>=1.30

google-genai>=1.40.0
